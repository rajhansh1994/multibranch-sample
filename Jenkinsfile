pipeline {
    agent any
     triggers {
       cron('* * * * *')
    stages {
        stage('Build') {
            steps { 
                echo "build is running"
                echo "build is successful"
            }
        }
    }
}
